import React from 'react';
import classes from '../../Layout/SectionSpace/SectionSpace.module.css';
const SectionSpace = (props) => {
   return <div className={classes.section__space} id={props.id}></div>;
};

export default SectionSpace;
